{
  "name": "velthiros",
  "version": "0.1.0",
  "private": true,
  "description": "Velthiros - a trial-based mobile action RPG. Playable HTML5 vertical slice built from the game design document.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "node tools/smoke.mjs",
    "serve": "npx --yes http-server . -p 8080 -c-1"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
